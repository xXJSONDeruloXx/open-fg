# 683320 — GRIS

## Identity
- AppID: `683320`
- Installed title: `GRIS`
- Observed launch path on Deck:
  - Proton launch into `/home/deck/.local/share/Steam/steamapps/common/GRIS/GRIS.exe`

## Why this title matters
- installed title that appears to launch through Proton, making it potentially more relevant to OMFG than native non-Vulkan titles
- useful as a medium-value coverage candidate once the generic harness is cleaner

## Current status
Status: **launch path observed, but OMFG engagement not yet proven**.

## Evidence
Initial generic smoke command:
- `OMFG_LAYER_MODE=passthrough OMFG_GAME_WAIT_SEC=45 ./scripts/test-steamdeck-steam-game.sh 683320 gris 'GRIS' '/GRIS$|GRIS.x86_64|GRIS.x86'`

Artifacts:
- `artifacts/steamdeck/rust/real-games/gris/passthrough/session.txt`

Observed process evidence:
```text
SteamLaunch AppId=683320 ... Proton 10.0/proton waitforexitandrun /home/deck/.local/share/Steam/steamapps/common/GRIS/GRIS.exe
python3 ... Proton 10.0/proton waitforexitandrun /home/deck/.local/share/Steam/steamapps/common/GRIS/GRIS.exe
```

Observed OMFG evidence:
```text
=== title=GRIS appid=683320 mode=passthrough omfg log ===
missing-log
```

Binary inspection evidence:
- `file ~/.local/share/Steam/steamapps/common/GRIS/GRIS.exe`
  - `PE32+ executable for MS Windows`
- `strings ~/.local/share/Steam/steamapps/common/GRIS/GRIS.exe | grep -i -m 10 'd3d11|vulkan|dxvk|vkd3d'`
  - included Vulkan-related strings such as:
    - `Vulkan`
    - `libvulkan.so.1`
    - `Failed to load bootstrap Vulkan functions.`

## Interpretation
- this title remains interesting because it is a Windows game under Proton and the shipped binary contains Vulkan-related strings
- however, the first generic passthrough probe still failed to produce OMFG layer logs
- current likely explanations are:
  - the title had not fully reached the Vulkan path inside the short observation window
  - the generic harness process filtering / cleanup was too noisy in the early version used for the run
  - the title uses a rendering path that is not hitting OMFG under current launch conditions

## Safer-harness rerun after self-kill fix
Rerun command:
- `OMFG_LAYER_MODE=passthrough OMFG_GAME_WAIT_SEC=45 ./scripts/test-steamdeck-steam-game.sh 683320 gris 'GRIS' 'GRIS.exe'`

Artifacts:
- `artifacts/steamdeck/rust/real-games/gris/passthrough/session.txt`
- `artifacts/steamdeck/rust/real-games/gris/passthrough/omfg.log`

Observed process evidence:
```text
SteamLaunch AppId=683320 ... Proton 10.0/proton waitforexitandrun /home/deck/.local/share/Steam/steamapps/common/GRIS/GRIS.exe
Z:\home\deck\.local\share\Steam\steamapps\common\GRIS\GRIS.exe
```

Observed OMFG evidence:
```text
app=SB-Win64-Shipping.exe; engine=DXVK
app=SB-Win64-Shipping.exe; engine=vkd3d
vkCreateDevice ok
vkCreateSwapchainKHR ok
vkQueuePresentKHR passthrough frame=2160
```

Interpretation:
- the generic harness self-kill bug is fixed: the remote session now survives, process evidence is captured, logs are copied back, and cleanup happens afterward
- however, the GRIS run produced a **title mismatch** in the OMFG log: the live process was `GRIS.exe`, but the captured Vulkan-layer log identified `SB-Win64-Shipping.exe`
- that means GRIS is still **not cleanly classified**; the current evidence suggests either stale/cross-title contamination or some other unexpected launch-path confusion on the Deck

## Next follow-up
- before further compatibility claims on GRIS, prove that the live process identity and OMFG log identity agree in the same run
- if mismatch persists, inspect wrapper log removal / launch ordering / Steam-side process overlap more deeply before using GRIS as a compatibility datapoint
